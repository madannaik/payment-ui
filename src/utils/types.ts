export type formdata = {
  email: string;
  cardNumber: string;
  cvv: number;
  exp: string;
  name: string;
  zip: number;
};

export type carddata = {
  cardNumber: string;
  name: string;
  exp: string;
  zip: number;
};
